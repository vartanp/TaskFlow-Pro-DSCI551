# TaskFlow Pro: SQLite B-Tree Indexing Demo

## Required Downloads
To run this application, you must have Python installed on your computer. You also need to download two specific Python libraries (Streamlit and Pandas). 

Open your terminal or command prompt (git bash is reccomeneded) and run this exact command to download them:
`pip install streamlit pandas`

## How to Run the Application
1. Download this project folder and open the folder with a working environment  like cursor or vs code **inside** the folder.
2. Launch the Streamlit dashboard by running in git bash:
   `python -m streamlit run app.py`
3. The application will automatically open in your default web browser at http://localhost:8501.

## Included Files
* `app.py`: The main application code and UI dashboard.
* `tasks_pro.db`: The SQLite database containing a synthetic dataset to demonstrate the B-Tree index functionality.
  
